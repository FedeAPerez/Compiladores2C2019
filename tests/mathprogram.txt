VAR
    [FLOAT, FLOAT, FLOAT, FLOAT, FLOAT] : [a, b, c, y, x]
ENDVAR
PRINT "Ingrese a:"
READ a
PRINT "Ingrese b"
READ b
PRINT "Ingrese c"
READ c
y := (a * a * x) + (b * x) + c
PRINT "El resultado es"
PRINT y